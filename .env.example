# Example environment file
#

# Postgres credentials (must match the existing pgdata volume)
POSTGRES_USER=rt
POSTGRES_PASSWORD=rt
POSTGRES_DB=rt

# Optional: if you set DATABASE_URL, keep it consistent with POSTGRES_*.
# DATABASE_URL=postgresql+psycopg://rt:rt@db:5432/rt

# Which pipeline to run (worker will load worker/pipelines/<PIPELINE>.yml)
PIPELINE=default

# Scanner toggles
ENABLE_NIKTO=1
ENABLE_NUCLEI=0
FAST_MODE=1

# API auth: required for POST /scan
# Format: comma/space/newline-separated list of <id>=<secret>
API_KEYS=local=changeme

# Required: only targets matching this allowlist can be scanned.
# Examples: juiceshop,example.com,1.2.3.0/24
SCAN_ALLOWLIST=juiceshop

# Override only for lab environments.
LAB_MODE=1

# Basic per-scan concurrency cap (clamps common tool concurrency args).
SCAN_CONCURRENCY_CAP=10

# Who launched scans (default if no X-Actor header)
DEFAULT_TRIGGERED_BY=local

# How scans are triggered (default for /scan endpoint)
DEFAULT_TRIGGERED_VIA=api
